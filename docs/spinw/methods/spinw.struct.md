(spinw-struct)=

# spinw.struct method

## Syntax
  
`objS = struct(obj)`
  
## Description
  
`objS = struct(obj)` converts all public properties of `obj` and saves
them into `objS` struct.
  
## See Also
  
[spinw](#spinw) \| [spinw.copy](#spinw-copy)
