(swpref-get)=

# swpref.get method

## Syntax
 
`value = get(obj, name)`
 
`value = obj.get(name)`
 
## Description
 
`value = get(obj, name)` gets the preference `name`.
 
`value = obj.get(name)` gets the preference `name`.
 
## See Also
 
[swpref.set](#swpref-set)
